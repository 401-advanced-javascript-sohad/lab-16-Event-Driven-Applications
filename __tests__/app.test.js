'use strict ';

// jest.mock('fs');


describe('File test', () => {
  it('can read the file', () => {
    expect(true).toBeTruthy();
  });
});



describe('Event emitting modules', () => {
  it('tests to come later', () => {
  });
});
